# Long-context routing fixture

Implement `route_case(ticket)` in `routing_policy.py`.

The detailed routing policy is intentionally not stored in this workspace. The benchmark checks whether external memory can recover the prior long-context policy.
