# Long Text Benchmark Fixture

This tiny repo is copied into each A/B condition workspace. The hidden rule archives are not committed here; benchmark setup injects them only through the memory path for condition B.

